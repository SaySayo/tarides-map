(executable
 (name server)
 (libraries dream yojson)
 (preprocess
  (pps lwt_ppx)))

(rule
 (targets server.ml)
 (deps server.eml.ml)
 (action
  (run dream_eml %{deps} --workspace %{workspace_root})))